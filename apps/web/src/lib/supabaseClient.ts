// This file is a re-exporter from the shared @dale/auth package.
// This keeps imports clean within the web app, e.g., `import { supabase } from '@/lib/supabaseClient';`
// or using relative paths like `import { supabase } from '../../lib/supabaseClient';`

export * from '@dale/auth';
